ANTHROPIC_API_KEY=
SYNC_TOKEN=change-me
PORT=3000
DB_PATH=./data/events.db
SESSION_PATH=./data/session
# Optional: set only if you need to link via phone number instead of
# scanning a QR (e.g. no second device available). Digits only, country
# code included, e.g. 972501234567. Leave blank to use the QR flow.
LINK_PHONE_NUMBER=
